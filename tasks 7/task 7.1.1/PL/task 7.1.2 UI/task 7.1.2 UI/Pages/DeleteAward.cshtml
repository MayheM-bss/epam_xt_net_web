@using task_7._1._2_UI;
@{
    if (IsPost)
    {
        var id = Request["AwardID"];
        if (Guid.TryParse(id, out Guid guid))
        {
            Dependencies.AwardManager.DeleteById(guid);
            Response.Redirect("~/");
        }
    }
}

